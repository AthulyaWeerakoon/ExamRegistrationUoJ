using Bunit;
using Xunit;
using ExamRegistrationUoJ.Components.Layout;

public class ExamRegLayoutTest
{
    [Fact]
    public void LayoutRendersCorrectly()
    {
        // Arrange
        using var context = new TestContext();

        // Act
        var component = context.RenderComponent<ExamRegLayout>();

        // Assert
        Assert.Contains("<div class=\"main-layout\">", component.Markup);
    }
}
