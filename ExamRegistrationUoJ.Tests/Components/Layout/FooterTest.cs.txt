using Bunit;
using Xunit;
using ExamRegistrationUoJ.Components.Layout;

public class FooterTest
{
    [Fact]
    public void FooterRendersCorrectly()
    {
        // Arrange
        using var context = new TestContext();

        // Act
        var component = context.RenderComponent<Footer>();

        // Assert
        Assert.Contains("&copy; 2023 University of Jaffna", component.Markup);
    }
}
